// if (read(read(cin, data1), data2))
// the if statement will first check the inner read call which will return a iostream which is then passed to the outer read function to read again to data2.
// The cin will be used consecutively to read the data required into 2 objects.
