#include <iostream>

int main () {
    // references need to be initialized, can't be null, are bound to the object they were initialized based on
    // pointers don't need to be initialized, can be null, can be used to manipulate memory, can do aritmetic operations, 
}