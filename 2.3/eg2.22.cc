/*
Exercise 2.22: Assuming p is a pointer to int, explain the following code: 
if (p) // will check if the pointer p references a valid memory address
if (*p) // the if condition will evaluate the dereferenced p = the value at the memory address.
*/