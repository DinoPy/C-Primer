/*
Exercise 2.23: Given a pointer p, can you determine whether p points to a valid object?
If so, how? If not, why not?

only by using the if condition can we see if the pointer is actually pointing to an address or is null but 
checking if it points to a valid object is not possible.
*/