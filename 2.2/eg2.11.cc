// extern int ix = 1024; - is a definition as we initialize the value so the external keyword is overwritten;
// int iy; - is a definition as well because we initialize the variable with a 0 or undifined value based on its location;
// extern int iz; - is a declaration and there is no initialization along with the extern keyword.