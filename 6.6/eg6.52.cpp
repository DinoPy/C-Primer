#include <iostream>

double dobj;
void mainip (int, int);
// mainif('a', 'z'); has a rank of 3 where there is a match through promotion.
// mainif(55.4, dobj); has a rank of 4 as both values are doubles and require arithmetic conversion.

using std::cout; using std::endl;

int main (int argc, char *argv[]) {
    return 0;
}
