
#include <iostream>
#include <string>

using std::string;
using std::cout; using std::cin; using std::endl;

int main() {
    // when reading a word at a time the space character is ignored jumping straight to the next word.
    // when reading a line we get the whole line except for \n character that's ignored
}