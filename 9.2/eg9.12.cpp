/*
 * A constructor that takes a container to copy, can only work if the new container is of the same type and has the same element type as the one that's being copied.
 * A constructor that takes the 2 begin and end iterators work on any container type except for array and can also do element conversion in case the original element type can be converted to the new container's element type.
*/
