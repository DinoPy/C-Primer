#include <vector>

using std::vector;


int main (int argc, char *argv[]) {
    vector<int> ivec;
    int num = 0;
    ivec[num];
    return 0;
}

// both int / const int / size_t can be used to subscript into a vector<int>.
