#include <iostream>

int main (int argc, char *argv[])
{
    // by using the prefix operator the operand would be first incremented, returned and then dereferenced for the print. This would cause the app to always print one ahead and always access one past the end of the array.
    return 0;
}
