#include <iostream>

int main (int argc, char *argv[])
{
    int j,i;
    double slope = static_cast<double>(j/i);
    // the difference resulted from j/i is cast as double and assigned to slope.
    return 0;
}
