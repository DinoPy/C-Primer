#include <iostream>

int main() {
    // in a logical AND && the operand on the left is evaluated first and only if the value is true then the next operand is evaluated.
    // in a logical OR || the operand on the left if evaluated first and only if the result is false then the right hand operator is evaluated.
}
