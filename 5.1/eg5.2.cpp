// a block is a sequence of statements located within curly braces. The block also has its own scope.
// A blck is required by functions, by loops, conditionals,  itis needed when we have to localize the usage of certain variables.
// usually the language syntax requires a single statement for most of its compoments however as the logic requires more than one then the statement is converted to a block (compound statement);
