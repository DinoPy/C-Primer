/*
 * map - associative container, the elements can be accesed via subscript however the type of the subscript does not have to be an integral value but the type of the key. it returns a pair that has first and second - key and value. Has unique keys.
 * vector - sequential container, hold memory contiguosly, the elements are accesed through index subscript or by going through the container.
 */
