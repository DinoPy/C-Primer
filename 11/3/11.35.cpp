/*
 * by using the subscript if we try to add the same key a second time we would
 * update its matching value. using insert this won't happen as it will return
 * false as it will return the already exisiting value.
 */
