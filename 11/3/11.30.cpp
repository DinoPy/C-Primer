/*
 * equal range returns a pair of iterators first will be lower_bound and second
 * is upper_bound. pos holds the pair; .first accesses the lower_bound iterator
 * ->second dereferences the iterator, and accesses the second property of
 * element of the map which is also a pair
 */
