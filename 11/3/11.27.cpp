/*
 * Count would be used to establish how many times a key occurs in a multi
 * container and possibly do a loop over those keys since they are adjacent.
 * Count can  be used to establish if a given key exist.
 */
