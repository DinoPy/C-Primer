// we passed value - 1 for readability and because we would need the new value received by the factorial call to be -1 of the current call, meaning val-- would not work as it would use the current value and reduce the value once the expression is executed, --val would have worked on the other hand.
//
