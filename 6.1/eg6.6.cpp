/*
 * a parameter is initialized from the arguments before the function receives control flow.
 * a local variable is initialized when the control flow reaches it and is destroyed when the control flow reaches the end of the block.
 * a static variable is initialized ahead of the control flow reaching the definition location and will be destroyed wheen the program ends.
 */
