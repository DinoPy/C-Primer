/*
 * a. the function return type doesn't match the type of the value that is returned. The function should indicate a return type of string or manipulate and return intigers
 * b. if f2 is a function declaration then it requires a return type, if f2 is a function call then it doesn't need a type but the argument type should match the parameter type.
 * c. the names of the parameters in calc function are identical. The parameter names must be unique hence they can't be same same.
 * d.  the return should be included in a block / {} so the function indentifies it as function body.
 */


