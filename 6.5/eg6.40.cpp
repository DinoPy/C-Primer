#include <iostream>

int ff(int a, int b = 0, int c = 0);
char *init (int ht = 24, int wd, char bckgrnd); // errored because the parameters receiving default arguments should be passed last.

int main (int argc, char *argv[]) {

    return 0;
}
