#include <iostream>
#include <vector>
#include <iterator>

int main (int argc, char *argv[]) {

    return 0;
}
class matrix {};

bool compare (matrix &val1, matrix &val2);
std::vector<int>::iterator change_val(int val1, std::vector<int> v1);
