// the resize operation that takes a single argument implies:
// That the type of value the container has, has a default constructor so the elements can be value initialized.
// That elements will be deleted if the new size is smaller that the current size of the container.
