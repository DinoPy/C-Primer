/*
 * a. is valid because const is a diferantiator between the type of arguments.
 * b. invalid as only the list of arguments and the name matters in diferentiating the function.
 * c. valid because the arguments are different.
 *
 */
